OS=linux # linux, windows
DEVICE=lab_system # lab_system, LP, OP, System
TEMP=0.5
OUTPUT_TOKENS=16 # -2 for full context, -1 for infinite
num_threads=2 # $(nproc -all)
PROMPT="Building a website can be done in 10 simple steps:\nStep 1:"
model_dir="../models"

